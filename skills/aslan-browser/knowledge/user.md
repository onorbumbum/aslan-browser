# User Learnings

User-specific preferences and workflows. Gitignored — not committed.

---

## Copy Preferences

- **Never use 👉 emoji.** User hates it. When linking to a URL in social copy, just put the URL on its own line — no emoji prefix.
